# Mobile Spire APK

Android WebView APK project for the mobile roguelike deckbuilder.

GitHub Actions builds a debug APK automatically on every push to `main`.

Download path after a successful run:

`Actions > Build Android APK > Artifacts > mobile-spire-debug-apk`
